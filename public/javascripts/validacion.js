function mostrarFormulario(tipo){
    // Agregramos
    if(tipo == "post"){
        window.location.href = "/formGet";
    }
    if(tipo == "get"){
        window.location.href = "/formGet";
    }
    if(tipo == "post"){
        window.location.href = "/formPost";
    }
}